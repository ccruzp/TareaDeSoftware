from django.conf.urls import patterns, include, url
from CLEI2014.views import home

#admin.autodiscover()

urlpatterns = patterns('',
    # Examples:
    # url(r'^$', 'CLEI2014.views.home', name='home'),
    # url(r'^blog/', include('blog.urls')),
    url(r'^$', home),
    #url(r'^admin/', include(admin.site.urls)),
)
